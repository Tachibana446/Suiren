<UserControl x:Class="Suiren.TLparts.TokenView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Suiren.TLparts"
        mc:Ignorable="d"
        Height="300" Width="300">
    <DockPanel x:Name="dockPanel">
        <Image DockPanel.Dock="Left" Width="100" Height="100" Source="{Binding ProfileImageUrl}"></Image>
        <TextBlock DockPanel.Dock="Top" Text="{Binding Name}" />
        <TextBlock DockPanel.Dock="Top" Text="{Binding ScreenName}" />
    </DockPanel>
</UserControl>
