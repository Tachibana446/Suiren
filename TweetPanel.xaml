<UserControl x:Class="Suiren.TweetPanel"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Suiren"
      mc:Ignorable="d" 
      d:DesignHeight="300"
      >
    <Border BorderBrush="Black" BorderThickness="1">
        <DockPanel Width="400">
            <Grid DockPanel.Dock="Left" Width="80" Height="80">
                <Image Width="80" Height="80" Source="{Binding IconUrl}" />
                <!-- リツイートの時表示 -->
                <Border x:Name="RetweetUserIconBorder" BorderThickness="1" BorderBrush="Black" Width="40" Height="40" Margin="40,40,0,0">
                    <Image Width="40" Height="40" x:Name="RetweetUserIcon"></Image>
                </Border>
            </Grid>
            <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Text="{Binding ShowName}"></TextBlock>
            <TextBlock DockPanel.Dock="Bottom" TextWrapping="Wrap" Text="{Binding CreatedAtStr}"></TextBlock>
            <TextBlock DockPanel.Dock="Bottom" TextWrapping="Wrap" Text="{Binding SentUserText}"></TextBlock>
            <TextBlock DockPanel.Dock="Top" TextWrapping="Wrap" Text="{Binding Text}"></TextBlock>
        </DockPanel>
    </Border>
</UserControl>
